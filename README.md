# Windows-Registry-Persistence-Detection-Splunk
A hands-on digital forensics project focused on detecting Windows Registry persistence mechanisms using Splunk. Includes log cleaning (Regex/SED) and threat hunting techniques to identify unauthorized auto-run entries.
